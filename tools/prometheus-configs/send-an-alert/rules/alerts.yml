---
groups:
- name: MyGroup
  rules:
  - alert: AlwaysAlert
    expr: 1
    labels:
      severity: ticket
      product: prometheus
    annotations:
      summary: "This is a test alert that always fires."
